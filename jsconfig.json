{
    "compilerOptions": {
        // Format: baseUrl/paths 배열 값
        // 맨 위부터 우선순위
        "baseUrl": ".",
        "paths": {
            "@/*": ["src/*"],
            "~/*": ["*"]
        }
    },
    "exclude": ["node_modules", "dist"]
}
