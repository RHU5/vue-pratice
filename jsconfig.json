{
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "~/*": ["./*"], // ~/ deco
            "@/*": ["./src/*"] // ~/ deco
        }
    },
    "exclude": ["node_modules", "dist"]
}
